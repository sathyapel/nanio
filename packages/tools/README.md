# @nanio/tools

Minimal, modular, and schema-validated functions for the `nanio` agentic framework.

## Key Features

*   **`BaseTool`**: Standard wrapper interface for exposing capabilities to LLMs.
*   **`FunctionTool`**: Dynamic tool wrapping using Zod schemas for strict argument validation and execution handling.
